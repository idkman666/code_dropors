import 'package:dropors/model/postLocation.dart';

class UserPost{
  String userName;
  String content;
  String pic;
  DateTime postDate;
  PostLocation postLocation;
  UserPost(this.userName, this.content, this.pic, this.postDate, this. postLocation);
}